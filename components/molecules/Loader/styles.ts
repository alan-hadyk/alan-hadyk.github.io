import { IThemeClasses } from "types/theme";

export const loaderDefaultThemeClasses: IThemeClasses = {
  height: "h-100%",
  position: "relative"
};

export const loaderSpinnerWrapperDefaultThemeClasses: IThemeClasses = {
  alignItems: "items-center",
  display: "flex",
  flexFlow: "flex-row-nowrap",
  height: "h-100%",
  justifyContent: "justify-center"
};
