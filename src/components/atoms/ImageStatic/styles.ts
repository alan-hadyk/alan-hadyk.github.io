import { IThemeClasses } from "@app/types/theme";

export const imageStaticDefaultThemeClasses: IThemeClasses = {
  height: "h-100%",
  transition: "transition-all",
  transitionDuration: "duration-veryFast",
  transitionTiming: "ease-in-out",
  width: "w-auto",
};
